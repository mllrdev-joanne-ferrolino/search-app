 query GetUsers($query: String!, $type: SearchType!) {
    search(query: $query, type: $type, first: 5) {
      nodes {
        ... on User {
          name
          login
        }
      }
    }
  }